{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "cd frontend && npx tsc --noEmit 2>&1 | head -20 || true",
            "description": "TypeScript check after every file edit"
          }
        ]
      }
    ]
  }
}
