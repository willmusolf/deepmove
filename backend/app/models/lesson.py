"""lesson.py — SQLAlchemy Lesson model"""
# TODO (Track D): Define Lesson model
# Fields: id, game_id (FK), user_id (FK), move_number,
#         principle_id, confidence, lesson_text, cached, created_at
