# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Database
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10

# Anthropic SDK
anthropic==0.39.0

# HTTP client (for Chess.com / Lichess proxying if needed)
httpx==0.28.1

# Config / env
pydantic-settings==2.6.1
python-dotenv==1.0.1

# Cache (in-memory LRU for MVP — no Redis needed yet)
cachetools==5.5.0

# Dev / testing
pytest==8.3.4
pytest-asyncio==0.24.0
httpx  # also used as async test client
ruff==0.8.3
