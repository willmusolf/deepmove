// CoachPanel.tsx — Coaching panel alongside the board
// Contains: LessonCard or SocraticPrompt (depending on Think First mode)
// Sits on the right side of the board on desktop, below on mobile.
// TODO (Track C): Wire up to useCoaching hook

export default function CoachPanel() {
  // TODO: Show LessonCard or SocraticPrompt based on mode and state
  return <div className="coach-panel" />
}
