// AccountLink.tsx — Username input for Chess.com or Lichess account linking
// Validates the username exists before saving.

export default function AccountLink() {
  return <div className="account-link" />
}
