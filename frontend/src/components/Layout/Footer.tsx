// Footer.tsx — Minimal footer
// Free tier: contains ad slot (single tasteful banner, never intrusive)
// Premium: ad slot hidden

export default function Footer() {
  return (
    <footer className="footer">
      {/* Ad slot — Carbon Ads / EthicalAds for free tier (Track D) */}
      <div id="ad-slot" />
    </footer>
  )
}
