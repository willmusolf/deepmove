// Header.tsx — Site header with logo and nav
// Clean, professional. No meme branding.

export default function Header() {
  return (
    <header className="header">
      <div className="header__logo">DeepMove</div>
      <nav className="header__nav">
        {/* TODO: Add nav links when routing is set up */}
      </nav>
    </header>
  )
}
