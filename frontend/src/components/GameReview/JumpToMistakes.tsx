// JumpToMistakes.tsx — "Jump to Mistakes" button and navigation
// Takes the user through ONLY the critical moments with coaching, skipping the rest.
// This is the fast path: import game → jump to coaching → learn.

export default function JumpToMistakes() {
  // TODO (Track C): Show next/prev critical moment buttons, trigger coaching panel
  return <button className="jump-to-mistakes">Jump to Mistakes</button>
}
