// ReviewPanel.tsx — Main game review layout
// Composes: ChessBoard + EvalBar + MoveList + CoachPanel
// This is the core screen of DeepMove.
// Board is centerpiece, coach panel on right (desktop) / below (mobile).

export default function ReviewPanel() {
  // TODO (Track C): Compose all review components with responsive layout
  return <div className="review-panel" />
}
